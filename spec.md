Produce a python script to run in GIMP 3.2.  There will be an image loaded into GIMP. 

1) If the file name has ".MP" in the name, remove these 3 characters.
2) Find the . in the file name, and replace it with "_gp.".
3) Export the image with a quality of 95.

The name of the script will be "S201b_Export"